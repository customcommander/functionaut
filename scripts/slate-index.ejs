---
search: true

language_tabs:
  - javascript

includes:
<% syms.sort((a, b) => a.name > b.name ? 1 : -1).forEach(sym => {%>
  - <%= sym.name %>
<%})%>
---

<%#

      Generates the content for docs/index.html.md
      ============================================

      WARNING:
      the --- --- section at the top MUST remain at the top!

%>

# <%= process.env.DOC_TITLE %>

<%- include('static/intro.md') %>